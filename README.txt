README

I implemented the DES by first converting the file into characters, then to vectors of the bits of the different characters. I found this to be an easier way to think about the different permutations, and the dealing with the S-boxes.